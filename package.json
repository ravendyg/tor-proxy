{
  "name": "gortrans-server-2",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": "npm i && tsd install && mkdir ./tor/data-dir && mkdir ./tor/data-dir/tor1 && mkdir ./tor/data-dir/tor2 && mkdir ./tor/data-dir/tor3 && mkdir ./tor/data-dir/tor4 && mkdir ./tor/data-dir/tor5",
    "start": "node ./bin/www"
  },
  "dependencies": {
    "bluebird": "3.4.6",
    "body-parser": "1.15.1",
    "cookie-parser": "1.4.3",
    "debug": "2.2.0",
    "express": "4.13.1",
    "jade": "1.11.0",
    "morgan": "1.7.0",
    "serve-favicon": "2.3.0",
    "socket.io": "1.4.8",
    "socks5-http-client": "1.0.2"
  },
  "devDependencies": {
    "eslint": "3.5.0"
  }
}
